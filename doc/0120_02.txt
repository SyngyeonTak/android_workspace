자바 언어에서 HTTP 통신을 이용하여 서버와 데이터를 주고 받을 수 있는가??
yes!!

안드로이드는 자바언어를 이용한다!!

안드로이드 버전이 업데이트 되면서 ,HTTPConnection이 먹지 않음

HTTP는 정보가 헤더에 노출된다(Post도 노출...)
HTTPS를 써야 된다.(암호화 서버가 필요하다.)

안드로이드의 대부분의 GUI 컴포넌트는 위젯이라 불리고
모든 위젯은 View의 자식이다.